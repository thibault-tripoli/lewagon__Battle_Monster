start_monster

<%= simple_form_for @monster do |f| %>
  <%= f.input :specie_id, as: :radio_buttons, collection: Specie.all, label: "choisis l'espece de ton monstre", include_blank: :empty %>
  <%= f.input :name,  label: "donne un nom a ton monstre" %>
  <%= f.button :submit %>
<% end %>
